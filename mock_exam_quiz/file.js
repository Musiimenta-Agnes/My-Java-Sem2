// What will be the output of of const = 3 + '3'

const number = 3 + '3'
console.log(number)//This pronts out '33' because one of the threes is a string therefore the output becomes a string.


// Calculate the number of values in this array.
let array = [1,2,3];
console.log(array.length)





